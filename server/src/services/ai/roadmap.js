const generateRoadmap = async (profile, gapAnalysis) => {
  // Simulate AI latency
  await new Promise(resolve => setTimeout(resolve, 800));

  // Real implementation will use Gemini API here
  return {
    phases: []
  };
};

module.exports = { generateRoadmap };
