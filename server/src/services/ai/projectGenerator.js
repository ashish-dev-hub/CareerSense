const generateProjects = async (profile, gapAnalysis) => {
  // Simulate AI latency
  await new Promise(resolve => setTimeout(resolve, 800));

  // Real implementation will use Gemini API here
  return {
    projects: []
  };
};

module.exports = { generateProjects };
